{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build && npx prisma generate"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy && echo '=== Migration complete ===' && echo '=== Starting Node.js server ===' && node dist/index.js 2>&1 || echo '=== ERROR: Server crashed with exit code' $? '==='",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
